<UserControl x:Class="Com.Scm.Uc.UcOAuth"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="240" d:DesignWidth="340">
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom">
            <Button x:Name="BtReturn"
                    Margin="10,5,10,10"
                    HorizontalAlignment="Stretch"
                    Style="{StaticResource ButtonPrimary}"
                    Click="BtReturn_Click">返回</Button>
        </StackPanel>
        <Grid Margin="10,10,10,5">
            <Border>
                <Image x:Name="PbLogo" Width="80" Height="80" />
            </Border>
            <Border Background="LightGray"
                    Opacity="0.3"
                    CornerRadius="5"
                    BorderThickness="1"
                    BorderBrush="Gray"></Border>
            <TextBlock x:Name="TbNotice" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center">adfadfadf</TextBlock>
        </Grid>
    </DockPanel>
</UserControl>
