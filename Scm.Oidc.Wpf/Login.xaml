<hc:Window x:Class="Com.Scm.Login"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           xmlns:hc="https://handyorg.github.io/handycontrol"
           WindowStartupLocation="CenterScreen"
           mc:Ignorable="d"
           Title="OIDC用户登录"
           ResizeMode="NoResize"
           Height="480" Width="320">
    <hc:Card BorderThickness="1"
             Effect="{StaticResource EffectShadow2}"
             Margin="8">
        <StackPanel Orientation="Vertical" Margin="15">
            <Grid>
                <Image Source="http://www.oidc.org.cn/logo/oidc_064.png" Height="64"/>
            </Grid>
            <Grid Margin="0,0,0,10">
                <TextBlock Style="{StaticResource TextBlockTitleBold}">用户登录</TextBlock>
            </Grid>
            <hc:ButtonGroup Margin="0,5">
                <RadioButton x:Name="RbPhone" Content="手机号码" Click="RbPhone_Click"/>
                <RadioButton x:Name="RbEmail" Content="电子邮件" Click="RbEmail_Click" IsChecked="True"/>
            </hc:ButtonGroup>
            <hc:ElementGroup Name="EgPhone" Margin="0,5" Visibility="Collapsed">
                <hc:ElementGroup.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </hc:ElementGroup.ItemsPanel>
                <Border Style="{StaticResource BorderRegion}" Padding="8,4">
                    <iconPacks:PackIconMaterial Kind="Phone" Width="16" Height="16" />
                </Border>
                <hc:TextBox x:Name="TbPhone"
                            Grid.Column="1"
                            HorizontalAlignment="Stretch"
                            hc:InfoElement.ShowClearButton="True"
                            hc:InfoElement.Placeholder="手机号码"/>
            </hc:ElementGroup>
            <hc:ElementGroup Name="EgEmail" Margin="0,5">
                <hc:ElementGroup.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </hc:ElementGroup.ItemsPanel>
                <Border Style="{StaticResource BorderRegion}" Padding="8,4">
                    <iconPacks:PackIconMaterial Kind="Email" Width="16" Height="16" />
                </Border>
                <hc:TextBox x:Name="TbEmail"
                            Grid.Column="1"
                            HorizontalAlignment="Stretch"
                            hc:InfoElement.ShowClearButton="True"
                            hc:InfoElement.Placeholder="电子邮件"/>
            </hc:ElementGroup>
            <hc:ElementGroup Margin="0,5">
                <hc:ElementGroup.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </hc:ElementGroup.ItemsPanel>
                <hc:TextBox x:Name="TbSms"
                            MaxHeight="6"
                            HorizontalAlignment="Stretch"
                            hc:InfoElement.ShowClearButton="True"
                            hc:InfoElement.Placeholder="验证码"/>
                <Button x:Name="BtSend"
                        Grid.Column="1"
                        Padding="15,0"
                        Content="发送"
                        Click="BtSend_Click"/>
            </hc:ElementGroup>
            <StackPanel Margin="0,5">
                <TextBlock x:Name="TbNotice" Foreground="Red">我们不会向任何第三方透露您的个人信息！</TextBlock>
            </StackPanel>
            <hc:ElementGroup Margin="0,5">
                <Button x:Name="BtVerify"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource ButtonPrimary}"
                        Content="登录"
                        Click="BtVerify_Click"/>
            </hc:ElementGroup>
            <hc:Divider Content="三方登录" Margin="0,10" HorizontalContentAlignment="Center"/>
            <StackPanel x:Name="SpOAuth" Orientation="Horizontal"></StackPanel>
            <Grid Margin="0,20,0,0">
                <TextBlock TextAlignment="Center">©<TextBlock x:Name="TbYear"></TextBlock> <Hyperlink Name="HlSite" NavigateUri="http://www.oidc.org.cn" Click="HlSite_Click">OIDC.org.cn</Hyperlink></TextBlock>
            </Grid>
        </StackPanel>
    </hc:Card>
</hc:Window>
